<Window x:Class="wpf_exemplo.históricoRelatorio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:wpf_exemplo"
        mc:Ignorable="d"
        Title="históricoRelatorio" Height="720" Width="1280"
        Background="Black">


    <Grid>
        <Image Source="/src/imagem_tcc.png"
               Stretch="UniformToFill"
               Opacity="0.85">
            <Image.Effect>
                <BlurEffect Radius="15"/>
            </Image.Effect>
        </Image>

        <Border Background="#AA000000" CornerRadius="30"
                Width="1000" Height="600"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Effect="{StaticResource MaterialDesignShadowDepth4}">

            <StackPanel Margin="40"
                        VerticalAlignment="Center">

                <materialDesign:PackIcon Kind="Historic"
                                         Width="30"
                                         Height="30"
                                         HorizontalAlignment="Center"
                                         />
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Histórico de Relatórios"
                                FontSize="28" FontWeight="Bold" Foreground="White"
                                HorizontalAlignment="Left" Margin="10,0,0,20"/>

                    <Border Grid.Row="1" Background="#33FFFFFF" CornerRadius="30" Height="50" Margin="0,0,0,20">
                        <Grid>
                            <materialDesign:PackIcon Kind="Magnify" Width="25" Height="25" 
                             VerticalAlignment="Center" Margin="15,0,0,0" Foreground="LightGray"/>

                            <TextBox x:Name="TxtPesquisa"
                                     TextChanged="TxtPesquisa_TextChanged"
                                     materialDesign:HintAssist.Hint="Pesquisar por Relatório"
                                     materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                                     BorderThickness="0"
                                     VerticalAlignment="Center"
                                     FontSize="16"
                                     Foreground="White"
                                     CaretBrush="White"
                                     Margin="50,0,15,0"/>
                        </Grid>
                    </Border>

                    <ListView x:Name="LvHistRelatorio" Grid.Row="2"
                      Background="Transparent"
                      BorderThickness="0"
                      HorizontalContentAlignment="Stretch"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      MouseDoubleClick="LvMoradores_MouseDoubleClick">
                        
                    </ListView>
                </Grid>
                    

            </StackPanel>
        </Border>
      <Grid/>
    </Grid>
</Window>
